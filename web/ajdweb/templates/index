{# -*- mode: jinja -*- #}
{% extends "base" %}

{% block content %}
 
 {% if url != '/' %}   
 {{ navigator(breadcrumb, titles[url]) }}
 {% endif %}

 <dl>
  {% for p in index | sort (attribute='date', reverse=True) %}
     <dt> 
        <a href="{{p.url}}">{{p.title}}</a>
        <span class='date'> {{ p.date.strftime('%d %b, %Y') }}</span> 
     </dt>
     <dd> {{ p.summary }} </dd>
  {% endfor %}
 </dl>

{% endblock %}
{# -*- mode: jinja -*- #}
{% extends "base" %}
{% block content %}
   <div class="grid infinite" > 
   {% for p in pages | sort (attribute='date', reverse=True) %}
     <div class="four columns grid-item page" >
       <span class="title"><a href="{{p.url}}">{{p.title}}</a> </span>
       <span class="date">{{ p.date.strftime('%d %b, %Y') }} </span>
       <span class="entry"> {{ p.summary }} </span>
      </div>
   {% endfor %}
   </div>
{% endblock %}